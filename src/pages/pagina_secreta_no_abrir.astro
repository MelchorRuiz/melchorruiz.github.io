<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Página Secreta</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&family=Source+Code+Pro:wght@400;700&display=swap"
            rel="stylesheet"
        />
        <style>
            body {
                font-family: "Inter", sans-serif;
            }

            /* Style for the secret message container */
            #secret-container {
                cursor: pointer; /* Show a pointer on desktop hover */
                -webkit-tap-highlight-color: transparent; /* Remove tap highlight on mobile */
            }

            /* The secret message is initially hidden by matching the background color */
            .secret-message {
                color: #111827; /* This should match the bg-gray-900 color */
                transition: color 0.5s ease-in-out; /* Smooth transition for the reveal */
            }

            /* This class is added by JavaScript to reveal the message */
            .secret-message.revealed {
                color: #e5e7eb; /* text-gray-200, a visible color */
            }

            /* The selection highlight still works as an alternative way to discover the message on desktop */
            ::selection {
                background-color: #fde047; /* yellow-300 */
                color: #1f2937; /* gray-800 */
            }
        </style>
    </head>
    <body
        class="flex min-h-screen items-center justify-center bg-gray-900 text-gray-200"
    >
        <div class="mx-auto max-w-lg p-8 text-center">
            <!-- Main Title -->
            <h1 class="mb-4 text-4xl font-bold text-white md:text-5xl">
                Página Ultra Secreta
            </h1>

            <!-- Updated instructions for the user -->
            <p class="mb-8 text-lg text-gray-400">
                Hay un mensaje oculto en esta página. <br />
                Toca o selecciona el espacio vacío de abajo para revelarlo.
            </p>

            <!-- The container for the secret message. It now has an ID to be targeted by JavaScript. -->
            <div id="secret-container" class="rounded-lg bg-gray-900 p-6">
                <p
                    id="secret-message"
                    class="secret-message font-mono text-2xl"
                >
                    Si lees esto, me debes unos tacos 🌮
                </p>
            </div>
        </div>

        <!-- JavaScript to handle the tap/click event -->
        <script>
            // Get the elements from the DOM
            const secretContainer = document.getElementById("secret-container") as HTMLElement;
            const secretMessage = document.getElementById("secret-message") as HTMLElement;

            // Add an event listener for clicks or taps on the container
            secretContainer.addEventListener("click", () => {
                // Add the 'revealed' class to the message to make it visible
                secretMessage.classList.add("revealed");
            });
        </script>
    </body>
</html>

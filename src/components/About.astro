---
import { getI18N } from "../i18n"
const { about } = getI18N(Astro.currentLocale);
---

<h2 class="text-2xl font-semibold py-2">{ about.title }</h2>
{
  about.description.map(paragraph => (
    <p class="py-1">{ paragraph }</p>
  ))
}